extends layout

block content
    .container.container.bg-white.mt-4.shadow
        div.row.d-flex.justify-content-center.align-items-center.h-100
            div.top-stripe.main-color
            .title.col-12.mt-4
                p.h2.text-center.mt-4.font-weight-bold You have finished the test with following results
        div.row.d-flex.justify-content-center.align-items-center
            canvas#myChart(style='width:100%;max-width:600px')

    script.
        var xValues = ["category A", "Category B", "Category C", "Category D", "Category E"];
        var yValues = [parseInt("#{rightA}"), parseInt("#{rightB}"), parseInt("#{rightC}"), parseInt("#{rightD}"), parseInt("#{rightE}")];
        var barColors = ["red", "green","blue","orange","brown"];

        new Chart("myChart", {
        type: "radar",
        data: {
            labels: xValues,
            datasets: [{
            label: "You",
            borderColor: "#6200ee",
            data: yValues
            }]
        },
        options: {
            legend: {display: false},   
            title: {
            display: true,
            text: "Your results",
            elements: {
                line: {
                borderWidth: 3
            }
            }
            }
        }
        });